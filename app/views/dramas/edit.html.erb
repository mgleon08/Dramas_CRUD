<%= form_for @drama, :url => drama_path(@drama),:method => :patch do |d| %>
    <%= render :partial => 'form', :locals => { :d => d } %>
    <%= d.submit "確定修改？", :class =>"btn btn-primary" %>
<% end %>
